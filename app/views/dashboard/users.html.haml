- @data.each do |office_name, users|
  %h2= office_name

  %table
    %thead
      %tr
        %th Name
        %th Email
        %th Role
        %th Invitation Status
        -# %th Actions
    %tbody
      - users.each do |user|
        %tr
          %td= "#{user}”
          %td= user.email
          %td= user.role
          %td= user.invitation_accepted_at ? 'Accepted' : 'Not yet accepted'
          -# %td
