-# Should this handled form logic in a form object/controller?
-# https://jaryl.medium.com/disciplined-rails-form-object-techniques-patterns-part-1-23cfffcaf429

ok if we submit a form, we get params in string, it might be an empty string but still a string. it is never nil

-# start_date <= end_date

-# how to add selected="selected" in office?
-# https://api.rubyonrails.org/classes/ActionView/Helpers/FormOptionsHelper.html#method-i-options_for_select

= form_with method: 'GET' do |f|
  = f.select :office_id, current_user.offices.map { |office| [office.name, office.id] } # what if there are no offices!!!
  %div
  = f.text_field :start_date, value: "#{@start_date}",
    data: { controller: "flatpickr", flatpickr_alt_format: t("date.formats.long"), flatpickr_alt_input: true }
  = f.text_field :end_date, value: "#{@end_date}",
    data: { controller: "flatpickr", flatpickr_alt_format: t("date.formats.long"), flatpickr_alt_input: true}
  = f.submit "Submit"
