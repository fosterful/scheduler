{
  "dockerComposeFile": ["docker-compose.yml", "docker-compose.codespaces.yml"],
  "service": "code",
  "workspaceFolder": "/workspaces/scheduler",
  "runServices": ["code", "postgres", "redis"],
  "postCreateCommand": "sudo chown -R $USER:$USER $(pwd) && bin/setup"
}
